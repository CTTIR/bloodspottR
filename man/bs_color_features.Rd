% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color.R
\name{bs_color_features}
\alias{bs_color_features}
\title{Extract the study's three relative optical-density color features}
\usage{
bs_color_features(rgb, white = 255, pseudocount = 1)
}
\arguments{
\item{rgb}{Numeric matrix with three columns, or array with dimensions
height by width by three channels. Channels must be ordered red, green, blue.}

\item{white}{Finite positive common channel white reference, default 255.}

\item{pseudocount}{Finite positive offset in input intensity units, default 1.}
}
\value{
Numeric object with the same dimensions as \code{rgb}, with its final
dimension named \code{od_g_minus_r}, \code{od_b_minus_r}, \code{od_mean}.
}
\description{
Computes channel OD as \code{-log((channel + pseudocount) / (white + pseudocount))}. Output channels are green OD minus red OD,
blue OD minus red OD, and mean channel OD, in that order. With the default
settings this is the three-feature definition used by the study's RGB/OD
networks, evaluated in double precision. The first two features are log
red-to-green and red-to-blue chromaticity ratios. These are relative digital
image features, not calibrated chromophore concentrations or fitted labels.
}
\details{
No rescaling, clipping, white estimation or statistical fitting is performed.
\code{white} is an explicit common channel reference in the input's intensity units.
All pixels must be finite and within \verb{[0, white]}; masks and missing pixels
must be handled explicitly before calling this function.
}
\examples{
bs_color_features(rbind(c(255, 255, 255), c(255, 127, 63)))
}
