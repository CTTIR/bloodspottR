% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden.R
\name{aggregate_burden}
\alias{aggregate_burden}
\title{Aggregate disjoint measurements using summed areas and counts}
\usage{
aggregate_burden(x, by = c("physical_slide_id", "organ"))
}
\arguments{
\item{x}{Data frame containing grouping columns, \code{physical_area_id},
\code{n_spots}, \code{n_profiles}, \code{red_area_mm2}, and \code{tissue_area_mm2}.
An optional character \code{qc_flags} column contains semicolon-separated flags;
an empty string means no concern marked in the reviewed material, while \code{NA}
means quality information is unavailable.}

\item{by}{Column names defining summaries; defaults to physical slide and organ.}
}
\value{
One data-frame row per group, with area-weighted fractions and densities.
}
\description{
Each \code{physical_area_id} must occur once. IDs are a safeguard, not proof that
two geometries are disjoint; scan overlap must be reconciled upstream.
Missing profile counts propagate to the aggregate rather than becoming zero.
The default produces descriptive slide-level summaries without requiring
animal identities or treatment labels. Unselected metadata are not returned.
A slide summary does not establish an independent biological replicate.
Optional \code{qc_flags} are carried into the report without filtering measurements
or changing denominators. Compromised input is processed like other input;
flags describe interpretation limits rather than validating the counts.
}
