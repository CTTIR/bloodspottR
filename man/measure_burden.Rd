% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden.R
\name{measure_burden}
\alias{measure_burden}
\title{Calculate calibrated burden from classified pixels and objects}
\usage{
measure_burden(
  n_spots,
  red_pixels,
  tissue_pixels,
  pixel_width_um,
  pixel_height_um,
  n_profiles = NULL
)
}
\arguments{
\item{n_spots}{Nonnegative whole spot counts.}

\item{red_pixels}{Nonnegative whole positive-pixel counts within tissue.}

\item{tissue_pixels}{Nonnegative whole evaluable-tissue pixel counts.}

\item{pixel_width_um, pixel_height_um}{Pixel dimensions in micrometres; either
one value or one per measurement.}

\item{n_profiles}{Optional nonnegative whole counts of resolved profiles.
Omit when profile counts are unavailable or unsupported.}
}
\value{
A data frame of areas in square millimetres, counts, fractions and
densities. The \code{n_profiles} and \code{profiles_per_mm2} columns are \code{NA} when absent.
}
\description{
Inputs must already refer to the same evaluable tissue mask. This function
checks units and arithmetic; it does not validate image segmentation.
Zero evaluable area produces undefined densities, represented by \code{NA}.
}
\examples{
measure_burden(10, 200, 10000, 0.25, 0.25)
}
